# DECLARE word, first_char, remaining_char, capitalized_word
# word = input("Enter a word: ")
# IF len(word) > 0:
#     first_char = word[0].upper()  # Convert first character to uppercase
#     remaining_chars = word[1:].lower()  # Convert remaining characters to lowercase
#     capitalized_word = first_char + remaining_chars
# ELSE:
#     capitalized_word = word  # Handle empty word case
# PRINT("Capitalized Word:", capitalized_word)
